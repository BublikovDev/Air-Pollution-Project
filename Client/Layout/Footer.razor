@if (visible)
{
        <div class="row mt-3">
            <div class="col center-block" style="color:#6B7472"><h2>We are on social networks:</h2></div>
        </div>
        <div class="row mt-2">
                <div class="col-12 center-block"><div><img class="me-4" src="/icons/NewFolder/image 1.svg"/><img class="me-4" src="/icons/NewFolder/image 2.svg"/><img class="me-4" src="/icons/NewFolder/image 3.svg"/></div></div>
        </div>
        <div class="row  mt-3">
            <div class="col-12 center-block" style="color:#6B7472">Year of creation: 2024. All rights reserved</div>
        </div>
}

@code{
    [Inject] NavigationManager NavigationManager { get; set; }

    private bool visible = true;

    protected override async Task OnInitializedAsync() 
    { 
        if(NavigationManager.Uri == $"{NavigationManager.BaseUri}signup" || NavigationManager.Uri == $"{NavigationManager.BaseUri}signin")
        {
            visible = false;
        }
        else
        {
            visible=true;
        }
    }
}